# How to run
Run the following commands
1. docker build -t branch-locator-app . (or choose name)
2. docker run -d -p8080:80 branch-locator-app (or choose port and use image name)
3. Then access at **http://localhost:8080/** (or chosen port)